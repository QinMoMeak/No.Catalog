package com.nocatalog.app.core.common

object Constants {
    const val DATABASE_NAME = "nocatalog.db"
    const val DATASTORE_NAME = "nocatalog.preferences"
    const val KEYSTORE_ALIAS = "nocatalog_keystore_key"
    const val DEFAULT_LOCK_TIMEOUT_SECONDS = 30
    const val DEFAULT_REMOTE_DIR = "/collection-app/"
    const val BACKUP_SCHEMA_VERSION = 1
}

